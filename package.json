{
  "name": "model-previewer",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-local STL inspection and measurement tool.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:cloudflare": "wrangler preview",
    "deploy:cloudflare": "wrangler deploy",
    "test": "node --test"
  },
  "dependencies": {
    "three": "0.183.1"
  },
  "devDependencies": {
    "vite": "8.3.0",
    "wrangler": "4.136.3"
  }
}
